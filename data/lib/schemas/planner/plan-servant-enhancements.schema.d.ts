import { Schema } from 'mongoose';
/**
 * Mongoose schema for the `PlanServantEnhancements` type.
 */
export declare const PlanServantEnhancementsSchema: Schema<any, import("mongoose").Model<any, any, any>, {}>;
